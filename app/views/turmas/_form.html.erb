<%= form_with(model: turma, local: true) do |form| %>
  <% if turma.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(turma.errors.count, "error") %> prohibited this turma from being saved:</h2>

      <ul>
      <% turma.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nome %>
    <%= form.text_field :nome %>
  </div>
  <div class="field">
    <%= form.label :sala_id %>
    <%= form.collection_select(:sala_id, Sala.all, :id, :salabloco, {:prompt=>"Selecionar Sala"}) %>
  </div>
  <div class="field">
    <%= form.label :hora_id %>
    <%= form.collection_select(:hora_id, Hora.all, :id, :iniciofim, {:prompt=>"Selecionar Hora"}) %>
  </div>


  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
